# Variables are containers for storing values such as strings, integers, floats, and booleans.
# The behavior of a variable depends on the value it contains.

# Example variable
first_name = "Sherrard"

# Print the value of the variable first_name
print(first_name)  # Outputs: Sherrard

# Print the string "first_name"
print("first_name")  # Outputs: first_name

# Print the value of first_name using an f-string for formatted output
print(f"{first_name}")  # Outputs: Sherrard
# The 'f' in f-string allows embedding variable values within strings.

# Strings are sequences of characters enclosed in single or double quotation marks.
# Examples of different types of strings:

# Letters (A-Z, a-z)
letters_example_1 = "Sherrard"
letters_example_2 = 'Sherrard'

# Numbers (0-9)
numbers_example_1 = "1234"
numbers_example_2 = '1234'

# Symbols
symbols_example = "@#$%"

# Emojis (shortcut: press Windows key + period)
emojis_example = "😀😁😂🤣"

# Punctuation marks
punctuation_example = ". , ! ? ; :"

# Whitespace (using spaces)
whitespace_example = "    "  # Four spaces
# Newline can be represented using triple single quotes
newline_example = ''' 
'''

# A character refers to any individual letter, number, symbol, or space that can be inputted.
# This includes:
# - Alphanumeric Characters: A-Z, a-z, 0-9
# - Special Characters: Punctuation marks (., !, ?, ;, etc.), Symbols (@, #, $, %, &, *, etc.)
# - Whitespace: Space (space bar), Tab (Tab key), Newline (Enter key)

# Best practice is to use double quotation marks for strings in Python,
# but both single and double quotes are valid.
